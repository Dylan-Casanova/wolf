{$DOMAIN:localhost} {
    # WebSocket — proxy /app/* to Reverb (must be before the catch-all)
    handle /app/* {
        reverse_proxy reverb:8080
    }

    # Laravel app — proxy to internal Nginx
    handle {
        reverse_proxy nginx:80
    }

    log {
        output stdout
    }
}
